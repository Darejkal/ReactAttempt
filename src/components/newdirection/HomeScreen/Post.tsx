import React from 'react'
import { View } from 'react-native';
type PostProps = { }
export const Post: React.FC<PostProps> = ({ }) => {
    return (
        <View>
            
        </View>
    )
};

export default Post;